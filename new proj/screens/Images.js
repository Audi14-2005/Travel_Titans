import * as React from "react";
import { StyleSheet, View } from "react-native";

const Images = () => {
  return <View style={styles.images1} />;
};

const styles = StyleSheet.create({
  images1: {
    flex: 1,
    width: "100%",
    height: 225,
  },
});

export default Images;
